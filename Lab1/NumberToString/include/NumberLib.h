#ifndef NUMBER_LIB_H
#define NUMBER_LIB_H

/**
 * @brief Converts a number to a string
 * 
 * @param number 
 * @return char* - Holds a reference to the name of each digit in one string
 */
char* NumToStr(int number);

#endif // NUMBER_LIB_H